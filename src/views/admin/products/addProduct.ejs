<!DOCTYPE html>
<html lang="es">
  <!-- HEAD-->
  <%- include("../partials/head.ejs") %>
  <body>
      <!-- dashboard admin -->
      <%- include("../partials/dashboard.ejs") %>
    <div class="admin-content">
      <%- include("../partials/header.ejs") %>

      <main id="main-dark">
        <div>
          <h1>Agregar Producto</h1>
          <form
            action="/admin/agregar"
            method="POST"
            enctype="multipart/form-data"
            style="margin-top: 5.5em"
            name="formAdd"
          >
             <!-- empieza caja con caracteristicas del producto-->
             <div class="form caja-agregar">
              Nombre:<label for="nombre-producto"
                ><input
                  class="input style-admin"
                  type="text"
                  name="name"
                  id="nombre-producto"
                  placeholder="Nombre del producto"
                />
                <% if (locals.errors && errors.name) { %>
                  <small class="text-danger"><%= errors.name.msg %></small>
                <% } %>
              </label>
              
              Marca:
              <label for="marca-producto">
                <select class="input style-admin" name="id_marca" id="marca_producto">
                <option>Seleccionar marca</option>
                <% brands.forEach(brand => { %>
                  <option value="<%- brand.id %>"><%- brand.name %></option>
                <% }) %>
                 </select>
                <% if (locals.errors && errors.id_marca) { %>
                  <small class="text-danger"><%= errors.id_marca.msg %></small>
                <% } %>
                </select>
              </label>

              Descripción:<label for="descripción-producto"
                ><input
                  id="descripción-producto"
                  cols="30"
                  rows="10"
                  class="input style-admin"
                  name="description"
                  placeholder="Descripción del producto"
                  />
                  <% if (locals.errors && errors.description) { %>
                    <small class="text-danger"><%= errors.description.msg %></small>
                  <% } %>
                </label>
              Categoría:<label for="categoria-producto">
                <select class="input style-admin categorySelect" name="categoria" id="categoria-producto">
                  <option selected hidden value="">Categorias</option>
                 <% locals.categories.forEach(category => { %>
                    <option value="<%- category.id %>" <%= locals.old && old.category == category.id ? "selected" : "" %> ><%- category.name %></option>
                 <% }) %>
                </select>
                <% if (locals.errors && errors.categoria) { %>
                    <small class="text-danger"><%= errors.categoria.msg %></small>
                <% } %>
                </select>
              </label>
              Subcategoría:<label for="subcategoria-producto">
                <select class="input style-admin subcategorySelect" id="subcategoria-producto" name="id_subcategory">
                  <option selected hidden value="">Subcategorias</option>

                </select>
                <% if (locals.errors && errors.id_subcategory) { %>
                    <small class="text-danger"><%= errors.id_subcategory.msg %></small>
                <% } %>
                </select>
              </label>
              Color:
              <label for="colores-producto">
                <select class="input style-admin" name="colour" id="colores-producto">
                <option value="">Seleccionar color</option>
                <% colours.forEach(colour => { %>
                  <option value="<%- colour.id %>"><%- colour.name %></option>
                <% }) %>
                 </select>
                 <% if (locals.errors && errors.colour) { %>
                  <small class="text-danger"><%= errors.colour.msg %></small>
              <% } %>
                </label>
              Talle:<label for="talles-producto">
                <select class="input style-admin" name="id_talle" id="talles-producto">
                  <option value="">Seleccionar talle</option>
                    <% talles.forEach(talle => { %>
                      <option value="<%- talle.id %>" name="id_talle"><%- talle.talle %></option>
                    <% }) %>
                  </select>
                  <% if (locals.errors && errors.id_talle) { %>
                    <small class="text-danger"><%= errors.id_talle.msg %></small>
                <% } %>
              </label>
              Precio:<label for="precio-producto">
                <input
                  id="precio-producto"
                  class="input style-admin"
                  type="number"
                  name="price"
                  placeholder="Modificar precio del producto"
                />
                <% if (locals.errors && errors.price) { %>
                  <small class="text-danger"><%= errors.price.msg %></small>
                <% } %>
              </label>
              Descuento:<label for="producto-discontinuo">
                <input
                  id="producto-discontinuo"
                  class="input style-admin"
                  name="discount"
                  placeholder="Descuento"
                  />
                  <% if (locals.errors && errors.discount) { %>
                    <small class="text-danger"><%= errors.discount.msg %></small>
                  <% } %>
              </label>
              Imagen:<label for="imagen-producto"
                ><input
                  id="imagen-producto"
                  class="input style-admin"
                  type="file"
                  name="image"
                  multiple
                />
                <% if (locals.errors && errors.image) { %>
                  <small class="text-danger"><%= errors.image.msg %> </small>
                <% } %>
              </label>
              Visible:<label for="producto-visible">
                <select class="input style-admin" name="visible" id="producto-visible">
                  <option value="1">Si</option>
                  <option value="0">No</option>
                </select>
              </label>
              Stock:<label for="stock">
                <input
                  id="stock"
                  class="input style-admin"
                  name="stock"
                  placeholder="Modificar stock"
                />
                <% if (locals.errors && errors.stock) { %>
                  <small class="text-danger"><%= errors.stock.msg %></small>
                <% } %>
              </label>
                <button type="submit" class="btn-admin style-admin" id="guardar">
                  GUARDAR
                </button>
            </div>
          </form>
        </div>
      </main>
    </div>
    <script src="/javascripts/adminCategories.js"></script>
  </body>
</html>
